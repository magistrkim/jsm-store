import React from "react";
type FormType = "sign-in" | "sign-up";

const AuthForm = ({ type }: { type: FormType }) => {
  return <div className="text-green">AuthForm</div>;
};

export default AuthForm;
